<nav class="navbar navbar-static-top navbar-grey">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if current_student %>
        <%= link_to("MsPan", student_root_path, { class: "navbar-brand" }) %>
      <% elsif current_teacher %>
        <%= link_to("MsPan", teacher_root_path, { class: "navbar-brand" }) %>
      <% elsif current_admin %>
        <%= link_to("MsPan", admin_root_path, { class: "navbar-brand" }) %>
      <% else %>
        <%= link_to("MsPan", root_path, { class: "navbar-brand", target: "_self" }) %>
      <% end %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <% if current_student %>
        <%= render "layouts/student_header_links" %>
      <% end %>
      <% if current_student %>
        <%= render "layouts/student_header_info" %>
      <% elsif current_teacher %>
        <%= render "layouts/teacher_header_info" %>
      <% elsif current_admin %>
        <%= render "layouts/admin_header_info" %>
      <% end %>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
